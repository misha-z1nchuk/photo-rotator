{
  "name": "photo-rotator",
  "version": "1.0.0",
  "description": "",
  "main": "lib/main.js",
  "type": "commonjs",
  "scripts": {
    "start": "NODE_ENV=production electron .",
    "test": "echo \"Error: no test specified\" && exit 1",
    "dist": "electron-builder",
    "test:lint": "npx eslint lib/main.js lib/preload.js lib/renderer.js",
    "test:lint:fix": "npx eslint --fix lib/main.js lib/preload.js lib/renderer.js"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "electron": "^18.1.0",
    "electron-builder": "^23.0.3",
    "eslint": "^8.14.0"
  },
  "build": {
    "appId": "photo.rotator",
    "productName": "photo rotator",
    "target": "AppImage",
    "directories": {
      "buildResources": "temp"
    }
  },
  "dependencies": {
    "dotenv": "^16.0.0",
    "imagemagick": "^0.1.3"
  }
}
